# End-to-End Validation

실행 전에 백엔드와 프런트엔드를 시작합니다.

```bash
npm run e2e
```

최소 검사:

- 작업 목록이 로드된다.
- 작업을 생성할 수 있다.
- 상태를 In Progress로 변경할 수 있다.
- 작업을 승인 요청 상태로 보낼 수 있다.
- 승인 후 상태가 Approved가 된다.
- 오류 메시지가 화면에 표시된다.
- 좁은 화면에서 의도하지 않은 페이지 가로 Scroll이 생기지 않는다.
