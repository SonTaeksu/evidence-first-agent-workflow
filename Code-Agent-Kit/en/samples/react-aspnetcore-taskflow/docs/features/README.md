# Feature State

Each feature has:

- one persistent current document;
- one append-only history;
- at most one active worklog.

Project Map is the routing index. Project-level current is a summary and does not replace feature current.
