# Validation Evidence

Generated evidence belongs under `generated/`.

Each record should include:

- command;
- working directory;
- start and end time;
- exit code;
- captured output;
- PASS or FAIL.

Do not manually convert a failing command into PASS.
