# Reference Images

Place original image files here without editing or recompression.

For each important image, optionally add:

- `<name>.regions.json`
- `<name>.compare.json`

Generate evidence before sending the image to an LLM.
