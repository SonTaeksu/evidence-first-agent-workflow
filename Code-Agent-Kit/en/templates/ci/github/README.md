# GitHub Actions Template

This workflow is an inactive sample.

Adapt it to the target project's stack, build, and test commands before copying it to `.github/workflows/`.
Review it first because the unchanged file may run commands for the included sample.
