# Verified Skeletons

Store known-good minimal framework artifacts here when schema, metadata, or version fields are easy to hallucinate.

- record source and version;
- preserve the original;
- copy and modify instead of inventing from memory;
- exclude confidential skeletons from public repositories.
