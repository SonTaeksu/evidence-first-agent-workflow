# check-kit-installation

Validates a single copied or installed language mirror.

```bash
python tools/check-kit-installation/check_kit_installation.py --root .
```

It checks required operational files, required directories, local Markdown links, and absence of `.ko.md` suffixes.
