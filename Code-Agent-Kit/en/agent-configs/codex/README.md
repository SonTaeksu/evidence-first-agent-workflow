# Codex setup

Codex uses:

- root `AGENTS.md`;
- project `.codex/config.toml`.

After trusting the project, restart Codex and run:

```text
/mcp
```

or:

```bash
codex mcp list
```

Configured servers:

- Microsoft Learn MCP
- Context7 MCP
