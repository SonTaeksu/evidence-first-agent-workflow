# Claude Code setup

Claude Code reads:

- `CLAUDE.md`
- root `AGENTS.md`
- project `.mcp.json`

Project-scoped MCP servers require approval on first use.

Check connections with:

```text
/mcp
```

Configured servers:

- Microsoft Learn MCP
- Context7 MCP
