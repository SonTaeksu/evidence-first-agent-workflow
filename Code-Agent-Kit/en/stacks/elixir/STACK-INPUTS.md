# Elixir Stack Inputs — Planned

This stack is intentionally **blocked** until an owner supplies or confirms:

- Elixir, OTP, Phoenix, and LiveView versions
- Context and boundary conventions
- Ecto repository and migration policy
- authentication and authorization
- LiveView versus controller ownership
- asset pipeline and deployment
- mix format, compile, test, and browser gates

Copy the complete stack contract from `stacks/_template/`, add primary-source provenance and skeletons, then run the readiness validator.
