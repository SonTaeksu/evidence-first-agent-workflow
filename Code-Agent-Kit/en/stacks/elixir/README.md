# Elixir + Phoenix Stack Profile — Planned

Candidate deterministic gates:

```bash
mix format --check-formatted
mix compile --warnings-as-errors
mix test
```

The profile will be added after the first public sample is validated.
