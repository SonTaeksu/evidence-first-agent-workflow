# Stack-Specific Agent Rules

This file extends root `AGENTS.md`.

- Read `STACK-READINESS.json` before stack-dependent implementation.
- A blocking capability with status `unknown` prohibits the dependent pattern.
- Use `SKILL.md` for selective reference routing.
- Use verified facts, provenance, and skeletons instead of memory.
- Follow the stack feature, artifact, communication, and validation contracts.
- Record every selected capability in Project Map and Gate Analysis.

## Stack Prohibitions

-
