# React and ASP.NET Core Skeleton Sources

The public sample itself is the known-good skeleton:

- `samples/react-aspnetcore-taskflow/frontend/`
- `samples/react-aspnetcore-taskflow/backend/`

Copy only the relevant minimal pattern. Do not copy sample versions or in-memory data policy into an adopted project without capability and owner confirmation.
