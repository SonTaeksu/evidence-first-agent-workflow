# Go + HTMX Stack Inputs — Planned

This stack is intentionally **blocked** until an owner supplies or confirms:

- supported Go and HTMX versions
- HTTP router and middleware ownership
- template engine and partial/fragment conventions
- database and migration tool
- authentication and CSRF policy
- asset pipeline and deployment command
- validation commands and golden HTML fragments

Copy the complete stack contract from `stacks/_template/`, add primary-source provenance and skeletons, then run the readiness validator.
