# Isolated Demos

Use `demos/<name>/` for experiments that must not modify production sample behavior.

Each demo must include:

- purpose;
- run commands;
- source evidence;
- validation result;
- explicit statement of any production files changed.

A useful demo is promoted through the normal new-feature workflow. Do not silently move experimental code into production.
